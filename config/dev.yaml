database:
  host: localhost
  port: 5432
  name: ai_service_db
  user: postgres
  password: guyanzhang123

redis:
  host: localhost
  port: 6379
  db: 0
  password: null
  
model:
  name: mistral
  base_url: http://localhost:11434

log:
  level: INFO        # DEBUG / INFO / WARNING / ERROR
  file: logs/app.log # 日志输出文件路径
  rotation: "10 MB"  # 单文件最大大小，超过后自动切分
  retention: "7 days" # 日志保留时间

api:
  wechat_token: "your_wechat_token"
  openai_key: "your_openai_api_key"
  third_party:
    weather_api_key: "xxxx"
    map_api_key: "xxxx"
